// Actividad 2
// En esta actividad, mejorarás la función anterior para que reciba la distancia y el tiempo como parámetros, lo que la hará más reutilizable.

// function calculate speed con los parámetros agregados
function calculateSpeed(distance, time){
    // Define una variable para el cálculo de la velocidad
    const velocidad = distance / time
    //Imprime en la consola el resultado.
    console.log(velocidad);
}
// Recuerda que para ejecutar una función hay que usar los paréntesis
calculateSpeed(10,2)
calculateSpeed(100,2)
calculateSpeed(75,3)
// Ejecuta la función tres veces. A modo de ejemplo: